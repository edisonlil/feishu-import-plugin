# 飞书多维表格Excel导入插件

## 功能概述

这是一个飞书多维表格的边栏插件，用于将Excel文件数据导入到当前多维表格中。

## 主要功能

### 1. 文件上传
- 支持多种Excel格式：`.xlsx`、`.xls`、`.csv`
- 文件大小限制：20MB
- 拖拽上传或点击上传

### 2. 列映射
- 自动识别Excel表头和多维表格列名
- 手动建立对应关系
- 防止重复映射
- 支持跳过不需要的列

### 3. 数据预览
- 显示前10行数据预览
- 确认映射关系正确性
- 查看即将导入的数据格式

### 4. 批量导入
- 支持大量数据导入
- 自动分批处理（每批200条）
- 实时显示导入进度
- 详细的成功/失败统计

## 使用流程

### 步骤1：上传Excel文件
1. 点击上传区域或拖拽Excel文件
2. 确认文件格式和大小符合要求
3. 点击"解析文件"按钮

### 步骤2：建立列对应关系
1. 查看Excel列名和多维表格列名
2. 为每个Excel列选择对应的多维表格列
3. 可以跳过不需要的列（不选择对应关系）
4. 点击"下一步"继续

### 步骤3：数据预览
1. 查看前10行数据的映射结果
2. 确认数据格式正确
3. 点击"确认导入"开始导入

### 步骤4：导入完成
1. 查看导入结果统计
2. 成功导入的记录数量
3. 可以点击"重新导入"开始新的导入流程

## 技术特性

### 性能优化
- 批量插入：每批200条记录，提高导入效率
- 进度显示：实时显示导入进度
- 错误处理：单条记录失败不影响整体导入

### 用户体验
- 步骤式引导：清晰的4步导入流程
- 响应式设计：适配不同屏幕尺寸
- 飞书风格：遵循飞书UI设计规范

### 数据处理
- 智能解析：自动识别Excel文件格式
- 灵活映射：支持任意列对应关系
- 数据预览：导入前确认数据正确性

## 注意事项

1. **文件格式**：优先支持`.xlsx`格式，也兼容`.xls`和`.csv`
2. **数据量**：建议单次导入不超过10000条记录
3. **列类型**：暂不支持自动数据类型转换，请确保数据格式匹配
4. **权限**：需要多维表格的编辑权限才能导入数据
5. **网络**：大量数据导入需要稳定的网络连接

## 错误处理

- **文件解析失败**：检查文件格式和文件完整性
- **列映射错误**：确保选择了正确的对应关系
- **导入失败**：检查多维表格列的数据类型是否匹配
- **网络错误**：检查网络连接，可以重试导入

## 开发信息

- **框架**：Vue 3 + Vite + Element Plus
- **SDK**：飞书多维表格 JavaScript SDK
- **Excel解析**：SheetJS (xlsx)
- **样式**：飞书设计规范，边栏插件优化
